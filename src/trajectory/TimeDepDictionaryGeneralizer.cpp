#include "TimeDepDictionaryGeneralizer.h"

using namespace std;
using namespace arma;
/*
TimeDepDictionaryGeneralizer::TimeDepDictionaryGeneralizer(vector<double> timeCenters, arma::vec initQueryPoint, ControlQueue* simulationQueue, ControlQueue* executionQueue, std::string dictionaryPath, int degOfFreedom, std::vector<double> tmpmys,
						std::vector<double> tmpsigmas, double az, double bz, double stepSize,
						double tolAbsErr, double tolRelErr, double ax, double tau, double ac, arma::vec trajMetricWeights,
                                                    double maxRelativeToMeanDistance, double as, double alpham) :
    DictionaryGeneralizer(initQueryPoint, simulationQueue, executionQueue, dictionaryPath, degOfFreedom, tmpmys, tmpsigmas, az, bz, stepSize, tolAbsErr, tolRelErr, ax, tau, ac, trajMetricWeights, maxRelativeToMeanDistance, as, alpham) {


    this->timeCenters = timeCenters;


}

TimeDepDictionaryGeneralizer::TimeDepDictionaryGeneralizer(vector<double> timeCenters, arma::vec initQueryPoint, ControlQueue* simulationQueue, ControlQueue* executionQueue, std::string dictionaryPath, int degOfFreedom, std::vector<double> tmpmys, std::vector<double> tmpsigmas, double az, double bz,
                  double stepSize, double tolAbsErr, double tolRelErr, double ax, double tau, double ac, double as, arma::mat metric, double maxRelativeToMeanDistance, double alpham)
    : DictionaryGeneralizer(initQueryPoint, simulationQueue, executionQueue, dictionaryPath, degOfFreedom, tmpmys, tmpsigmas, az, bz, stepSize, tolAbsErr, tolRelErr, ax, tau, ac, as, metric, maxRelativeToMeanDistance, alpham) {
	

    this->timeCenters = timeCenters;
	
}
*/
