#ifndef CUSTOMSET
#define CUSTOMSET

#include <math.h>
#include <vector>
#include <armadillo>

class CustomSet {

private:
	
	std::vector<arma::vec> intSet;
	
public:
	
	CustomSet();
	
	int insert(arma::vec vec1);
	int find(arma::vec vec1);
	
	int compareArmadilloVec(arma::vec vec1, arma::vec vec2);
	
};

#endif