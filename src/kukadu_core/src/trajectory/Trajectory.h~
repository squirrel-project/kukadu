#ifndef TRAJECTORY
#define TRAJECTORY

#include <vector>
#include <armadillo>

class Trajectory {
	
private:
	
	arma::vec supervisedTs;
	std::vector<arma::vec> sampleYs;
	
public:
	
	Trajectory(arma::vec supervisedTs, std::vector<arma::vec> sampleYs);
	
	int getDegreesOfFreedom();
	
	arma::vec getSupervisedTs();
	arma::vec getSamplYByIndex(int x);
	
	
};

#endif